import { FC } from 'react';

const CartPage: FC = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
      }}
    >
      Тут будут товары добавленные в корзину
    </div>
  );
};
export default CartPage;
